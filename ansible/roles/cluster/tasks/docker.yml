---
- name: install docker
  yum: name=docker-io state=present

- name: add local docker group with consistent gid
  group: name=docker_cluster gid=10000

- name: use local docker group
  lineinfile:
    dest: /etc/sysconfig/docker
    regexp: ^other_args=
    line: other_args="-G docker_cluster"
  notify: restart docker

- name: start docker
  service: name=docker state=started
